# Dependencies for beta-graph (also in pyproject.toml).
# Use: python3 -m pip install --user -r requirements.txt
# Then: python3 -m pip install --user -e .
# Or just: python3 -m pip install --user -e .  (installs these automatically)

fastmcp>=2.0,<3.0
langchain-mcp-adapters>=0.1.0
geopy>=2.4.0
langchain>=0.3.0
langgraph>=0.2.0
langchain-google-genai>=2.0.0
chromadb>=0.4.0
sentence-transformers>=2.2.0
requests>=2.31.0
beautifulsoup4>=4.12.0
pydantic>=2.0.0
